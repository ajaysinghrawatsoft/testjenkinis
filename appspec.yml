version: 0.0
os: linux
files:
  - source: /
    destination: /
permissions:
  - object: /
    pattern: "**"
    owner: www-data
    group: www-data
    type:
      - directory
      - file
hooks:
  ApplicationStop:
    - location: /home/ec2-user/getpull.sh
      timeout: 30
      runas: root
  BeforeInstall:
    - location: /home/ec2-user/getpull.sh
      timeout: 300
      runas: root
  AfterInstall:
    - location: /home/ec2-user/getpull.sh
      timeout: 300
      runas: www-data
  ApplicationStart:
    - location: /home/ec2-user/getpull.sh
      timeout: 30
      runas: root